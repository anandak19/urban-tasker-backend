import { MultiImageValidatorPipe } from './multi-image-validator.pipe';

describe('MultiImageValidatorPipe', () => {
  it('should be defined', () => {
    expect(new MultiImageValidatorPipe()).toBeDefined();
  });
});
